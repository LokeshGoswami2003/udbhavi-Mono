# File Responsibility Map

Date: 2026-05-03

This report explains what each current source/config/doc file is doing. It separates tracked source from generated or local-only artifacts.

## Root Files

| File | Responsibility | Notes |
| --- | --- | --- |
| `AGENTS.md` | Repository working contract for Codex and future agents. | Important because it defines local URLs, modularity rules, and safe logging expectations. |

## Client Files

### Client package and build config

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/package.json` | Defines client scripts and dependencies. | Scripts are `dev`, `build`, `lint`, and `preview`. Uses React, Vite, Tailwind, Auth0 React SDK, and lucide icons. |
| `client/package-lock.json` | Locks installed client dependency versions. | Generated by npm; should change only when dependencies change. |
| `client/vite.config.js` | Configures Vite with React and Tailwind plugins. | Minimal and correct for current frontend. |
| `client/eslint.config.js` | Configures ESLint for JS/JSX, React hooks, and Vite refresh rules. | Good lightweight lint setup. |
| `client/index.html` | Browser HTML shell for Vite. | Title is still `client`; should be changed to `Udbhavi`. |
| `client/.gitignore` | Ignores logs, build output, node modules, and local env files. | Correctly ignores `.env.local` through `*.local`. |
| `client/.env.example` | Public client env template. | Contains Vite Auth0/API public values. Must stay public-only. |
| `client/README.md` | Client setup and Auth0 local flow notes. | Useful, but says server should later validate tokens even though server validation now exists. |

### Client entry, routing, and auth

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/src/main.jsx` | React entrypoint. Wraps `App` in `AppAuthProvider`. | Clean and small. |
| `client/src/App.jsx` | Current app root, manual route switcher, and theme owner. | Works now, but owns too many concerns for the next phase. |
| `client/src/router/navigation.js` | Tiny browser history helper. | Supports manual SPA navigation through custom events. No query/hash handling beyond browser history itself. |
| `client/src/auth/auth-config.js` | Reads Auth0/Vite env config and builds redirect/logout URLs. | Good minimal central config. |
| `client/src/auth/AuthProvider.jsx` | Wraps app with Auth0Provider when config exists. | Uses localStorage token cache; this is a security tradeoff. |
| `client/src/auth/use-auth-actions.js` | Provides login, signup, logout, and API token helpers. | Keeps Auth0 SDK calls out of page components. |
| `client/src/api/api-client.js` | Calls `/api/v1/me` with a bearer token. | Throws a generic error and drops response code/request ID. |

### Client pages

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/src/pages/landing/LandingPage.jsx` | Composes the landing page sections. | Clean composition file. |
| `client/src/pages/auth/AuthRedirectPage.jsx` | Redirects `/login` and `/signup` into Auth0 Universal Login. | Needs rejection/callback error handling. |
| `client/src/pages/auth/AuthSetupPage.jsx` | Shows local setup guidance when Auth0 client env is missing. | Helpful in development; should not appear as-is in production. |
| `client/src/pages/workspace/WorkspacePage.jsx` | Protected workspace gate and first workspace shell. Calls `/me`. | Correctly waits for backend account sync, but `New resume` is a dead action. |

### Client shared UI

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/src/components/ui/Button.jsx` | Shared button/link styling variants. | Useful primitive. Consider validating variants or defaulting safely if variants grow. |
| `client/src/components/ui/SectionLabel.jsx` | Small pill label used in landing sections. | Simple and reusable. |

### Client landing components

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/src/lib/landing-data.js` | Central content arrays and icon references for landing sections. | Good separation of content from layout. Watch for fictional proof/testimonial copy. |
| `client/src/components/landing/LandingHeader.jsx` | Sticky landing header with nav, auth links, theme toggle, and mobile menu icon. | Mobile menu button has no behavior. |
| `client/src/components/landing/HeroSection.jsx` | First landing viewport with headline, CTAs, badges, and resume mockup. | Strong product signal. Uses decorative blurred backgrounds. |
| `client/src/components/landing/ResumeMockup.jsx` | Visual fake product/editor/resume preview for hero. | Helpful visual asset, but it is static mock data. |
| `client/src/components/landing/HowItWorksSection.jsx` | Four-step workflow section. | Some copy explains implementation control rather than user value. |
| `client/src/components/landing/FeatureSection.jsx` | Feature grid. | Some copy is future-tense/product-plan oriented. |
| `client/src/components/landing/ProductPreviewSection.jsx` | Static editor preview and CTA. | Useful for direction, but not connected to real app state. |
| `client/src/components/landing/TemplatesSection.jsx` | Template preview cards and CTA. | Includes templates that do not exist yet. |
| `client/src/components/landing/TrustSection.jsx` | Privacy/security positioning section. | Claims should stay aligned with implemented deletion/privacy controls as product grows. |
| `client/src/components/landing/TestimonialsSection.jsx` | Static testimonial cards. | Risky if testimonials are not real. Consider replacing with product principles until real proof exists. |
| `client/src/components/landing/PricingCtaSection.jsx` | Free-tier CTA section. | Current free-tier claims are planned, not enforced yet. |
| `client/src/components/landing/FaqSection.jsx` | FAQ accordion using native `details`. | Simple and accessible baseline. |
| `client/src/components/landing/LandingFooter.jsx` | Footer links and email form. | Email form has no submit behavior. Footer privacy/terms links point to `#top`. |
| `client/src/components/landing/ThemeToggle.jsx` | Theme toggle button. | Good isolated control. |

### Client styling and assets

| File | Responsibility | Notes |
| --- | --- | --- |
| `client/src/index.css` | Tailwind import, theme variant, base styles, surface helper classes. | Works, but surface helper classes are ad hoc and should not grow into a hidden design system. |
| `client/public/favicon.svg` | Browser favicon. | Public asset. |
| `client/public/icons.svg` | Public icon asset. | Check whether this is still used before keeping long term. |
| `client/src/assets/hero.png` | Product/landing visual asset. | It is currently tracked but not referenced in the read source files. |
| `client/src/assets/react.svg` | Vite starter asset. | Stale if unused. Remove when cleaning assets. |
| `client/src/assets/vite.svg` | Vite starter asset. | Stale if unused. Remove when cleaning assets. |

## Server Files

### Server package and config

| File | Responsibility | Notes |
| --- | --- | --- |
| `server/package.json` | Defines server scripts and dependencies. | `test` intentionally fails; this is a verification gap. |
| `server/package-lock.json` | Locks installed server dependency versions. | Generated by npm. |
| `server/.gitignore` | Ignores server env files, logs, node modules, and build output. | Correctly keeps real `.env` out of git. |
| `server/.env.example` | Server env template. | Good shape. Keep credentials as placeholders only. |
| `server/src/config/env.js` | Loads and normalizes server environment values. | Only requires `MONGODB_URI`; Auth0 missing config is found later at request time. |
| `server/src/config/db.js` | Connects/disconnects Mongoose and reports DB status. | Does not log full connection strings, which is good. |

### Server app and utilities

| File | Responsibility | Notes |
| --- | --- | --- |
| `server/src/server.js` | Runtime entrypoint. Connects DB, creates HTTP server, listens, handles shutdown. | Good minimal entrypoint. Could improve shutdown timeout/error handling later. |
| `server/src/app.js` | Express app factory and middleware/route registration. | Clean composition file. Good for future tests once env friction is handled. |
| `server/src/utils/logger.js` | Structured JSON logger with levels and Error formatting. | Useful and minimal. Avoid logging raw provider errors with secrets later. |

### Server middleware

| File | Responsibility | Notes |
| --- | --- | --- |
| `server/src/middleware/request-id.js` | Assigns or accepts request ID and sets `x-request-id`. | Good for traceability. Could validate/sanitize inbound ID length. |
| `server/src/middleware/rate-limit.js` | IP-based Express rate limiter. | Good first layer. Not enough for future user quotas. |
| `server/src/middleware/auth.js` | Auth0 API JWT validation middleware factory. | Correct provider choice. Should fail fast or explicitly support local bypass. |
| `server/src/middleware/sync-user.js` | Creates/updates local user after JWT validation and attaches `req.user`. | Good module boundary. Needs tests around missing `sub` and DB failures. |
| `server/src/middleware/not-found.js` | Returns JSON 404 for unmatched routes. | Echoes original URL. Safe enough now, but avoid exposing query values later. |
| `server/src/middleware/error.js` | Shared JSON error response and structured error logging. | Better than raw logs. Future provider/upload/AI errors need strict sanitizers. |

### Server modules

| File | Responsibility | Notes |
| --- | --- | --- |
| `server/src/modules/health/health.routes.js` | Registers `GET /health`. | Public route. |
| `server/src/modules/health/health.controller.js` | Formats health response. | Thin controller. |
| `server/src/modules/health/health.service.js` | Builds health payload from DB status and process uptime. | Uses process uptime directly; fine. |
| `server/src/modules/users/user.model.js` | Mongoose user schema with Auth0 subject, email, name, timestamps. | Minimal. Needs length/format constraints if user data grows. |
| `server/src/modules/users/user.routes.js` | Registers `GET /me` with `syncUser`. | Relies on parent route to run `authenticateJwt` first. |
| `server/src/modules/users/user.controller.js` | Formats `/me` response. | Thin controller. |
| `server/src/modules/users/user.service.js` | Claim extraction, profile cleanup, Mongo user upsert, response shaping. | Correct place for user sync rules. Needs duplicate-key and verification policy handling later. |

## Docs Files

| File | Responsibility | Notes |
| --- | --- | --- |
| `docs/ResumeBuilderPlan.md` | Full MVP architecture and roadmap. | Larger than current implementation. Use as plan, not current-state truth. |
| `docs/Auth0UserClaimsAction.md` | Auth0 Post Login Action guide for adding email/name claims to access tokens. | Important for `/me` profile sync quality. |
| `docs/AUDIT_FINDINGS.md` | Existing quality backlog from earlier audit pass. | Some items have since been addressed; still useful backlog. |
| `docs/LandingPage.png` | Landing page reference screenshot. | Useful if it is the canonical reference. |
| `docs/.gitignore` | Ignores `credentials.md`. | Good, but local secret files still need careful handling. |
| `docs/credentials.md` | Local-only credentials note, ignored by git. | Contains raw secrets. Treat as exposed local secret material and do not copy into reports. |

## Tracked Artifacts And Hygiene

Tracked files that look like temporary screenshots or starter assets:

- `client/landing-desktop.png`
- `client/landing-mobile.png`
- `client/landing-mobile-2.png`
- `client/landing-mobile-3.png`
- `client/landing-tall.png`
- `client/landing-tall-fixed.png`
- `client/landing-tall-final.png`
- `client/landing-tall-restarted.png`
- `client/src/assets/react.svg`
- `client/src/assets/vite.svg`

Local ignored files/directories observed:

- `client/dist`
- `client/node_modules`
- `server/node_modules`
- client log files
- client Chrome/CDP check folders
- real `.env` files
- `docs/credentials.md`

Recommendation:

- Keep only one canonical screenshot in `docs/` if needed.
- Remove unused Vite starter assets.
- Keep client root free of generated verification images.
- Keep local credentials out of normal docs and rotate any credentials that were stored in plaintext.

